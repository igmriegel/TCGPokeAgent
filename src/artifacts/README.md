# Artifacts

Models, frozen candidates, and submission `.tar.gz` files under `submissions/`, each with SHA-256, effective config, deck, SDK, and source run.

A package only enters here after isolated validation described in [`docs/13_eval_implementation.md`](../../docs/13_eval_implementation.md).
